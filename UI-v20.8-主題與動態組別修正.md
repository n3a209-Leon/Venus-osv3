# v20.8 主題與動態組別修正

## 修正內容

- 慕夏圖示改為「版本化外部 WebP → 內嵌 WebP → 文字符號」三級備援。
- 保留既有緊湊介面與單層慕夏外緣花邊，不為裝飾增加任何空間。
- 手機單日工具列外框左右各延伸 2px，讓它與下方表格的視覺寬度更一致。
- 作業表左上角不顯示「作業」文字。
- 移除自動產生的 `ungrouped` 欄位。
- 組別標題依設定順序顯示為數字 1～N；設定 5／6／7 組時，表格就只顯示 5／6／7 組。
- 表格欄寬、狀態徽章與新增作業列的跨欄數皆依組數動態計算。

## 相容性

- App 版本：16.0.8
- 部署版本：v20.8
- 資料格式：schema v6（未變更）
- Firebase 專案、班級 ID、IndexedDB 與 localStorage 鍵值均未變更
- 可由 v20.7 直接更新，不會清除或搬移既有資料

## 部署確認

完整覆蓋上傳後，畫面左上角必須顯示 `v16.0.8`。請保留 `assets` 目錄；即使外部鳶尾圖載入失敗，App 仍會自動使用內嵌圖片。
