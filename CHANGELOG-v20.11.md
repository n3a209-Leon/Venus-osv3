# v20.11 標題列裝飾框

## 修正

1. **標題列改為完整裝飾框**
   `.hi-row1` 本身變成一個框：LIMU 標題、版號、快捷鈕、主題鈕、齒輪鈕全部包在框內。
   花頭移到整列的最左與最右端（`::after`），框線與底色由 `::before` 繪製，
   中央那條只保留連接用細線，避免一列出現四朵花。
   - 慕夏：霧金框線＋鳶尾端飾
   - 星雲：金紫框線＋星雲端飾＋中央金點
   - NERV：六角切角橘框＋虛線規尺
   - 一般：維持原本的白色細髮線，不加框

2. **頁首底部花邊不再壓到月份列**
   `header::before`（慕夏）與 `header::after`（星雲）原本錨在 header 底部，
   而月份列正好在那個位置，導致花邊蓋住「‹」按鈕與同步狀態文字。
   改為錨在 header 頂端、降低不透明度，讓它退為背景紋理。

3. **班級列與狀態列端飾改為貼齊邊界**
   原本 `bottom:-4px` / `-3px` / `-1px` 會突出到下一個區塊，
   改為 `bottom:0` 並縮小、降透明度，成為該列的收邊而不是浮在中間。
   視覺層級：標題框（最強）→ 班級列與狀態列（收邊）→ 月曆外框。

## 相容性

- App 版本：16.0.11／部署版本 v20.11／快取 `hw-tracker-v20-11`
- 資料格式 schema v6，未變更；Firebase、班級 ID、IndexedDB／localStorage 鍵值均未變更
- 純 CSS 調整，未更動任何資料流程

## 部署確認

整個資料夾（含 `assets`）拖放到 Netlify，左上角須顯示 `v16.0.11`。

## 追加修正：更新提示訊息的版本號寫死問題

`index.html` 的 Service Worker 註冊區有多處把版本號寫死成字面字串
（`v20.8`、`16.0.x`），升版時不會跟著更新，導致 v20.10 部署後
警告列仍顯示「請重新上傳完整 v20.8」，讓人誤判成部署失敗。

已改為動態推導：
- `SW_LABEL` 由 `SW_BUILD_ID` 以正則取出（`limu-teacher-v20-11-…` → `v20.11`）
- App 版本標籤改讀 `window.__LIMU_BUILD__.appVersion`

之後升版只要改三檔的版本常數，所有提示訊息會自動跟上。

## 部署一致性自檢

本包出貨前已驗證：
- version.json：`limu-teacher-v20-11-20260727` / `hw-tracker-v20-11` / `16.0.11`
- index.html 同時包含上述 buildId 與 cacheName
- sw.js 同時包含上述 buildId 與 cacheName，且 DEPLOYMENT_MARKER 完整
