# LIMU v18 UI 介面評估

評估基準：手機直向、單手優先、畫面利用率高、常用操作最短、資料狀態清楚，而且不改變既有資料流程。

| 介面 | v18 已完成 | 下一步建議 | 優先度 |
|---|---|---|---|
| 全域導覽／標題列 | 手機底部加入「月曆、今天、搜尋、輔導、更多」；手機標題列移除重複捷徑 | 班級切換可再加入最近使用排序，但不建議增加更多常駐圖示 | 高 |
| 月曆首頁 | 保留今天、資料定位器、空白月份提示；快速切月加入舊請求防覆蓋 | 將資料定位器改成「一行摘要＋展開詳情」，讓月曆首屏再多露出一列；可加入左右滑月，但需避免與 iPhone 返回手勢衝突 | 高 |
| 單日作業紀錄 | 底部導覽可隨時回月曆／搜尋／輔導；彈窗不再捲動穿透 | 將最常用的「新增作業」做成靠右下的主要按鈕；頂端診斷、獎卡、同步、週計畫改收進次要工具列，保留復原／重做 | 最高 |
| 今日工作台 | 手機改為底部工作表；出勤、作業、行事與輔導維持同頁 | 作業區可只先顯示待處理項目，已完成項目收合；將「開啟完整紀錄」固定在面板底部 | 高 |
| 全域搜尋 | 手機改為底部工作表；有限併發加快歷年資料搜尋 | 加入最近 5 次搜尋與「座號」快速鍵；結果可依班級／月份分組，避免結果多時難掃讀 | 中高 |
| 輔導紀錄中心 | 底部導覽固定入口；彈窗開啟時鎖定背景 | 首頁先顯示「需追蹤」而非最近全部；新增紀錄的座號、分類、內容應維持同一畫面，避免再拆步驟 | 高 |
| 週作業計畫 | 保留上下學期與本週跳轉、容器獨立捲動 | 手機版預設用「每週卡片」，表格留給橫向或列印；儲存列固定底部並顯示未儲存週數 | 高 |
| 歷年檔案 | 統計與多班雲端定位改為有限併發；原完整刪除仍只在班級標題 | 「封存／備份／還原／清理」應分成日常與進階兩組；完整刪除維持紅色且不要放入更多選單 | 中高 |
| 報表／學生分析 | 本版不改資料與報表內容，以免跳過 v17 後混入大型報表重構 | 個別學生建議用時間軸＋月趨勢；全班建議用需要關注清單＋折線，不再增加雷達圖密度；列印需固定 A4 頁首、期間與匿名模式 | 中高 |
| 班級設定 | 班級／組別／幹部分頁改為黏附式；底部導覽保留 | 班級頁內容很長，建議再拆成「基本資料、學期日期、行事曆、進階」摺疊區；儲存按鈕應固定底部且顯示是否有未儲存變更 | 最高 |
| 組別設定 | 分頁可隨時切換；不改既有版本資料 | 座號配置適合改成拖曳或「先選座號、再指派組別」雙模式；手機預設仍以點選模式較安全 | 中 |
| 幹部設定 | 分頁可隨時切換；保留上／下學期 | 常用職務顯示成兩欄快速卡片；已指派座號應直接顯示學生狀態，避免重複指派 | 中 |
| 行事曆匯入 | 主要彈窗納入捲動鎖；沿用先勾選再加入 | 匯入流程最適合維持「來源→預覽→確認」三段；來源失敗時只顯示一個最建議的替代方法 | 中 |
| 同步衝突／診斷 | 同步衝突手機改為底部工作表；背景不穿透 | 衝突卡片先顯示日期、資料類型與差異摘要，再提供本機／雲端；技術診斷維持進階入口，不占日常畫面 | 中 |
| 登入／啟動畫面 | 本版維持既有流程，避免影響 Firebase 登入 | 若網路慢，直接顯示「正在載入離線資料／正在連線」兩段狀態，比單一轉圈更能降低不安 | 中 |

## 建議實作順序

1. 單日作業紀錄的主要／次要工具重新分層。
2. 設定頁固定儲存列與四個摺疊區。
3. 月曆資料定位器縮成一行摘要。
4. 今日工作台只優先顯示待處理項目。
5. 週計畫手機卡片模式。

這五項都只調整顯示與操作，不需要改動 Firebase、班級 ID、學年度規則、封存格式或現有資料鍵。
