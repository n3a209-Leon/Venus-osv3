# LIMU v20 UI、主題質感與後續建議

評估基準：iPhone 直向、單手優先、畫面利用率高、資料身分永遠清楚，並維持既有 Firebase、班級 ID 與 schema v6。

| 介面 | v20 已完成 | 下一步建議 | 優先度 |
|---|---|---|---|
| 全域班級定位 | 42～48px 固定定位列；顯示學年度、班級、資料天數與同步狀態；點擊展開完整定位並切班 | 班級很多時可依最近使用排序；不增加更多常駐圖示 | 中 |
| 全域導覽／標題列 | 手機移除重複班級列與帳號列；底部保留月曆、今天、搜尋、輔導、更多 | 「更多」可加入最近兩個功能，但維持五格底部導覽不變 | 低 |
| 月曆首頁 | 移除大型定位卡；固定列接管定位；新增慕夏多層框與 Nebula 金紫角框，日期格仍維持清楚 | 可加入從螢幕中段左右滑月；左右邊緣需保留給 iPhone 返回手勢 | 中 |
| 單日作業紀錄 | 工具重新分層；手機只常駐同步、週計畫、復原、重做；診斷與獎卡掃描移入更多 | 下一步最值得新增「＋作業」右下主按鈕，並在新增後自動捲到新列 | 高 |
| 今日工作台 | 底部工作表、批次標記與 30 秒復原維持；慕夏主題使用鳶尾花分隔飾帶與象牙紙卡片 | 預設先顯示待處理作業，已完成項目收合；底部固定「開啟完整紀錄」 | 高 |
| 全域搜尋 | 資料鍵預篩、有限併發、分段讓出主執行緒、舊搜尋防覆蓋 | 加入最近 5 次搜尋與座號快速鍵；結果依班級與月份折疊 | 中高 |
| 輔導紀錄中心 | 固定底部入口、背景捲動鎖與新版面板質感 | 首頁先顯示「需追蹤」；新增紀錄仍維持座號、分類、內容同一畫面 | 高 |
| 週作業計畫 | 上下學期、本週跳轉與獨立捲動保留 | 手機預設改為每週卡片；橫向表格保留給平板與列印；儲存列固定底部 | 高 |
| 歷年檔案 | 統計有限併發、班級定位與完整刪除安全邏輯維持；離屏區塊延遲繪製 | 封存／備份／還原列為日常；版本清理與完整刪除列為進階 | 中高 |
| 報表／學生分析 | 本版未動資料分析模型，避免混入已跳過的 v17 大型報表重構 | 個別學生採時間軸＋月趨勢；全班採關注清單＋折線；列印固定 A4 頁首、期間與匿名模式 | 中高 |
| 班級設定 | 分頁黏附、固定班級列可隨時確認正在編輯哪一班 | 拆成基本資料、學期日期、行事曆、進階四個摺疊區；固定狀態列顯示「已儲存／儲存中／失敗」 | 最高 |
| 組別設定 | 點選模式與歷史版本保留 | 增加「先選座號、再指派組別」批次模式；手機不預設拖曳，避免誤放 | 中 |
| 幹部設定 | 上下學期、職務快速選項與現有資料維持 | 常用職務改兩欄卡片；同座號重複指派時立即提示 | 中 |
| 行事曆匯入 | 先勾選再加入、排除清單與捲動鎖維持 | 明確分成來源、預覽、確認三段；來源失敗只突出一個最建議的替代方法 | 中 |
| 同步衝突／診斷 | 技術診斷已退出單日日常工具列；同步狀態常駐於班級定位列 | 衝突卡優先顯示日期、類型與差異摘要，再顯示本機／雲端選擇 | 中 |
| 登入／啟動畫面 | 字型不再阻塞首屏；PWA 背景與主題色已同步新版色系 | 慢網路時分兩段顯示「載入本機資料」與「連線雲端」，降低等待不確定感 | 中 |

## 建議下一輪順序

1. 班級設定四區摺疊＋固定儲存狀態。
2. 今日工作台優先顯示待處理項目。
3. 單日作業新增右下主按鈕。
4. 週計畫手機卡片模式與固定儲存列。
5. 搜尋最近紀錄與結果分組。

這五項只會調整操作與顯示，不需改動 Firebase、班級 ID、學年度規則、封存格式或既有資料鍵。
