# LIMU v20.1 上半部裝飾精修

> 本文件保留 v20.1 的上半部避讓設計紀錄；v20.2 已在相同安全原則下改用藍紫鳶尾 WebP 邊飾，最新規格請見 `UI-藍紫鳶尾裝飾整合.md`。

## 問題判斷

原本 Nebula 的大型日輪／拱門以 `header` 背景方式置中，會橫跨資料還原、輔導筆數、月份切換與「今天」按鈕。雖然裝飾層不接收點擊，仍會降低文字與控制項的輪廓辨識。

## 新的裝飾原則

1. **中央淨空**：文字、狀態與按鈕後方不放徽章、日輪或植物主體。
2. **邊界承載**：裝飾只出現在卡片邊框、群組轉場、左右端點與最外側留白。
3. **狀態色優先**：綠、橘、紅仍只代表同步或資料狀態；霧金只作裝飾。
4. **不增加高度**：新飾件使用偽元素與 SVG 邊飾，不新增佔版面的裝飾列。
5. **不攔截操作**：所有裝飾層均使用 `pointer-events:none`，並位於內容層下方。

## 實際調整

- 品牌頁首：大型中央拱門改為貼底的左右植物邊飾。
- 班級定位：中央鳶尾徽章改為兩側漸隱的細線飾帶。
- 狀態資訊：行事曆提醒、資料還原、輔導筆數集中為一個清楚的狀態群組。
- 月份列：加入低對比外框及左右端點，不在月份、今天按鈕或同步狀態後方放圖。
- 底部導覽：改用中央留白的邊飾，避免中央花徽靠近主要導覽標籤。
- 月曆內容：保留低透明度外框，但不在日期格內新增任何裝飾。

## 效能與安全

新資產 `mucha-edgework.svg` 為純向量、無動畫、無腳本；只在 Mucha 與 Nebula 主題使用。Firebase、班級 ID、schema v6、資料同步與 iPhone 安全區均未更動。
