# v20.10 標題列花邊（含 v20.9 裝飾層修復）

## v20.9 修復（已併入本版）

v20.5 曾加入一段「回歸 v19 緊湊結構」的 CSS，用 `!important` 把慕夏／星雲主題的所有裝飾層一次關閉。
它寫在樣式表最後、選擇器權重又相同，因此前面所有花邊規則全部失效，新主題只剩 `main::before` 一層外框，
看起來就像「主題沒有生效」；內嵌的 11 張 WebP 有 8 張完全讀不到。

- 移除封殺區塊（`display:none!important` 與 `content:none!important` 兩段）
- 恢復頁首端飾、班級列飾帶、狀態區飾帶、月曆與月摘要角框、底部導覽金飾帶、
  彈窗與主題面板卡片外框、面板橫向分隔飾帶
- 補上缺少的 DOM：`renderMuchaEmpty()` 現在會產生 `mucha-empty-art` 空狀態花飾
- 四主題各自的向量圖示：日輪／星雲漩渦／NERV 六角章／藍紫鳶尾（前三者為 inline SVG，零體積）
- `sw.js` 的 `PRECACHE_URLS` 補上 `./assets/signature-blue-iris.webp`

## v20.10 調整

v20.9 的獨立飾帶會在狀態區與月份列之間開一列，桌機 42px、手機 38px，是純粹的版面成本。
本版改為把花邊放進 LIMU 標題列原本就存在的 flex 空白：

```
[LIMU] [v16.0.10] [←── 原本的空白，現在是花邊 ──→] [快捷鈕][主題][⚙️]
```

- 該列高度由既有 32px 按鈕決定，花邊**不增加任何列高**
- 手機版快捷鈕本來就隱藏，空白更寬，花邊更完整
- `pointer-events:none`，不影響標題點擊回月曆

四個主題各有專屬收邊：

| 主題 | 標題列花邊 |
|---|---|
| 一般 | 白色細髮線漸層 |
| 慕夏 | 左右鳶尾端飾＋霧金細線 |
| 星雲 | 左右星雲端飾＋金點＋金紫細線 |
| NERV | 橘色虛線技術規尺 |

獨立的 `theme-ornament-band` 已停止產生（CSS 保留，日後要恢復只需重新掛上元素）。

## 尚未使用的素材

`--limu-art-corner`（約 53 KB）仍為零引用，維持 v20.4 文件所述「後續局部角花來源」的保留狀態。

## 相容性

- App 版本：16.0.10／部署版本 v20.10／快取 `hw-tracker-v20-10`
- 資料格式：schema v6，未變更
- Firebase 專案、班級 ID、IndexedDB 與 localStorage 鍵值均未變更
- 可由 v20.8 或 v20.9 直接更新，不會清除或搬移既有資料

## 部署確認

整個資料夾（含 `assets`）一起拖放到 Netlify，畫面左上角須顯示 `v16.0.10`。
