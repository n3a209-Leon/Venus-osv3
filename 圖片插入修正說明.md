# LIMU v20.4 圖片顯示與 iPhone 換版修正

## 根因

這次實機畫面左上仍顯示 `v16.0.2`，表示 v20.3 並未真正接管 iPhone；舊 Service Worker 與舊 App Shell 仍在運作。

同時，v20.2／v20.3 的美術層還有三個顯示問題：

- Nebula 手機版把裝飾降到 `11%`～`20%`，並將完整花飾縮成約 `13px`～`30px`。
- 主外框與卡片框位於不透明內容下方，圖片雖已載入，仍只剩極窄邊縫可見。
- 慕夏星期列的多層背景把逗號寫成分號，瀏覽器會丟棄該段背景宣告。

## v20.4 修正

- Service Worker 使用 `updateViaCache: none`，安裝完成後自動 `skipWaiting`、接管頁面並移除 v20.2／v20.3 舊快取。
- 導覽強制先讀取網路且驗證 `buildId`；伺服器若短暫回傳舊 HTML，改用已驗證的 v20.4 App Shell。
- 新增獨立鳶尾飾帶，放在狀態區與月份列之間；沒有任何按鈕或文字，不會與操作競爭。
- 慕夏主外框與卡片框改用只繪製外緣的 `border-image` 切片；中央完全透明。
- 卡片側框控制在約 `7px`～`10px`，不壓住月曆日期。
- Nebula 使用獨立燻金藍紫鳶尾邊飾，手機版透明度與尺寸提高到可辨識範圍。
- 空白月份的鳶尾裝飾在 Mucha 與 Nebula 都可見。
- 所有裝飾均為 `pointer-events: none`。

## 資料與相容性

本版只修改換版機制與美術顯示層，沒有變更 Firebase 專案、Firestore 路徑、班級 ID、IndexedDB、localStorage 鍵名或 schema v6。

部署後請確認左上顯示 `v16.0.4`。只要仍顯示 `v16.0.2` 或 `v16.0.3`，就代表手機尚未切換到本部署包。
